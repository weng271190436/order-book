{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/websocketpp",
            "cwd": "${workspaceFolder}",
            "args": [],
            "stopAtEntry": false,
        }
    ]
}