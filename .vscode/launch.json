{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/boost-websocket",
            "cwd": "${workspaceFolder}",
            "args": ["ws-feed.pro.coinbase.com", "443", "Hello, world!"],
            "stopAtEntry": true,
        }
    ]
}